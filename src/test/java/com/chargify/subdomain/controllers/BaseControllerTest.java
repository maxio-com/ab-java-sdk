/*
 * MaxioAdvancedBillingFormerlyChargifyAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.chargify.subdomain.controllers;

import com.chargify.subdomain.Environment;
import com.chargify.subdomain.MaxioAdvancedBillingFormerlyChargifyAPIClient;
import com.chargify.subdomain.testing.HttpCallbackCatcher;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.rules.Timeout;

/**
 * Base class for all test cases.
 */
public class BaseControllerTest {
    /**
     * Test configuration.
     */
    public static final int REQUEST_TIMEOUT = 30;

    public static final double ASSERT_PRECISION = 0.01;
    
    /**
     * Global rules for tests.
     */
    @Rule
    public Timeout globalTimeout = Timeout.seconds(REQUEST_TIMEOUT);
    
    /**
     * Test fixtures,
     * Used to serve as HttpCallback and to capture request & response.
     */

    protected static HttpCallbackCatcher httpResponse;
    
    /**
     * Setup test.
     */
    @BeforeClass
    public static void setUp() throws Exception {
        httpResponse = new HttpCallbackCatcher(); 
    }

    /**
     * Tear down test.
     */
    @AfterClass
    public static void tearDown() throws Exception {
        httpResponse = null;
    }

    /**
     * Create test configuration from Environment variables.
     */
    protected static MaxioAdvancedBillingFormerlyChargifyAPIClient createConfigurationFromEnvironment() {
        MaxioAdvancedBillingFormerlyChargifyAPIClient.Builder builder =
                new MaxioAdvancedBillingFormerlyChargifyAPIClient.Builder();

        final String environment =
                System.getenv("MAXIO_ADVANCED_BILLING_FORMERLY_CHARGIFY_API_LIB_ENVIRONMENT");
        final String subdomain =
                System.getenv("MAXIO_ADVANCED_BILLING_FORMERLY_CHARGIFY_API_LIB_SUBDOMAIN");
        final String timeout =
                System.getenv("MAXIO_ADVANCED_BILLING_FORMERLY_CHARGIFY_API_LIB_TIMEOUT");
        final String basicAuthUserName =
                System.getenv("MAXIO_ADVANCED_BILLING_FORMERLY_CHARGIFY_API_LIB_BASIC_AUTH_USER_NAME");
        final String basicAuthPassword =
                System.getenv("MAXIO_ADVANCED_BILLING_FORMERLY_CHARGIFY_API_LIB_BASIC_AUTH_PASSWORD");

        if (basicAuthUserName != null && basicAuthPassword != null) {
            builder.basicAuthCredentials(basicAuthUserName, basicAuthPassword);
        }
        if (environment != null) {
            builder.environment(Environment.fromString(environment));
        }
        if (subdomain != null) {
            builder.subdomain(subdomain);
        }
        if (timeout != null) {
            builder.httpClientConfig(configBuilder -> configBuilder.timeout(
                    Long.parseLong(timeout)));
        }
        return builder.build();
    }

    
    /**
     * Create test configuration.
     */
    protected static MaxioAdvancedBillingFormerlyChargifyAPIClient createConfiguration() {
        // Set Configuration parameters for test execution
        MaxioAdvancedBillingFormerlyChargifyAPIClient config =
                createConfigurationFromEnvironment();
        config = config.newBuilder()
                .httpCallback(httpResponse)
                .build();
        return config;
    }
}
